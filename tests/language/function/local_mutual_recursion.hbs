{
  var even = fn(n) {
    if (n == 0) return true;
    return odd(n - 1); // expect runtime error: Undefined variable 'odd'
  };

  var odd = fn(n) {
    if (n == 0) return false;
    return even(n - 1);
  };

  even(4);
}
